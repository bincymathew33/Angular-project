import { MovieGenerPipe } from './movie-gener.pipe';

describe('MovieGenerPipe', () => {
  it('create an instance', () => {
    const pipe = new MovieGenerPipe();
    expect(pipe).toBeTruthy();
  });
});
